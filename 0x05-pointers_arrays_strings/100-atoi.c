#include "main.h"
/**
 * _atoi - converts a string to an integer
 * @s: user input string / stdin (0)
 * Return: integer
 */
int _atoi(char *s)
{
	unsigned int i = 0;
	
	while (s[i] !- '\0')
	{
		if (s[i] == '-')
			_putchar(s[i]);
		if (s[i] >= 48 && s[i] <= 57)
			_putchar[s[i]];
	}
	return (0);	
}
