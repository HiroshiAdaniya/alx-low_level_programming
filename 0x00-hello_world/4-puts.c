#include <stdio.h>
/**
 * This prints out a string of characters using puts
 * Main entry point
 * return always 0 (success)
 */
int main(void)
{
	puts("Programming is like a multilingual puzzle", \n);
	return (0);
}
